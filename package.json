{
  "name": "agents-and-skills",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "lint": "pnpm -r run lint",
    "lint:fix": "pnpm -r run lint:fix",
    "format": "pnpm -r run format",
    "format:fix": "pnpm -r run format:fix",
    "test:unit": "pnpm -r run test:unit",
    "lint-staged": "pnpx lint-staged",
    "check:uncommitted": "node --experimental-strip-types scripts/check-uncommitted.ts"
  },
  "devDependencies": {
    "@types/micromatch": "^4.0.10",
    "husky": "^9.1.0",
    "lint-staged": "^16.1.0",
    "micromatch": "^4.0.8"
  },
  "engines": {
    "node": ">=22"
  }
}
