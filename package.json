{
  "name": "agents-and-skills",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "lint": "pnpm -r run lint",
    "lint:fix": "pnpm -r run lint:fix",
    "format": "pnpm -r run format",
    "format:fix": "pnpm -r run format:fix",
    "test:unit": "pnpm -r run test:unit",
    "lint-staged": "pnpx lint-staged",
    "lint:uncommitted": "lint-uncommitted",
    "hooks:install": "pnpm --filter lint-uncommitted run install-hook"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "@types/unist": "^3.0.3",
    "gray-matter": "^4.0.3",
    "husky": "^9.1.0",
    "lint-staged": "^16.1.0",
    "lint-uncommitted": "workspace:*",
    "parse-diff": "^0.11.1",
    "remark-parse": "^11.0.0",
    "unified": "^11.0.5",
    "unist-util-visit": "^5.1.0"
  },
  "engines": {
    "node": ">=22"
  }
}
